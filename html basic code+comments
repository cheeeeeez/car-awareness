<!DOCTYPE html>  <!-- html은 위에 !DOCTYPE으로 시작-->
<!-- html에 주석 표시하는 방법-->
<!-- <head>요소는 HTML 문서에 대한 정보를 정의한다.-->
<!-- <body>요소는 문서의 본문을 정의한다.-->
<!-- 아래에 상세하게 재서술했으며, 나머지 추가 테이블 요소들은 밑에 정의-->
<!-- 주의사항: 태그는 교차해서는 안된다!-->
<html> 
    <!-- HTML 문서는 <html>과 </html>로 둘러싸인다. 
    브라우저에게 이 파일의 내용이 HTML이라고 선언한다. -->
    <head> <!-- 헤드는 <head>와 </head> 태그로 구성되며 웹 페이지에 대한 정보를 저장하고 있는 곳이다.-->
       <title> 페이지 연습 </title> <!-- 웹 페이지 상단에 뜨는 제목 정하기-->
    </head>
    <body> <!-- 웹페이지의 모든 내용을 표시하는 곳, 브라우저 안에 표시되는 부분
        (사이트 틀면 보이는 모든 내용들이 body라고 생각하면 된다.)-->
        <p>캡스톤 디자인</p> <!--p요소는 단락을 나타낸다.--> <!-- p요소는 종료 태그가 없어도 되고, 사이트에 글을 표시할 때 쓴다.-->
        <h1>캡스톤</h1> <!-- 글씨 크기가 <h1>이 제일 크고, <h2>, <h3>으로 갈수록 작아진다. h는 heading의 약자이다.-->    
        <h2>캡스톤</h2>
                <!-- 텍스트 서식에도 여러가지가 있다. css가 디자인하는 언어라 훨씬 더 다양하지만, html로도 구현이 가능하다.-->
                <p><b>이 텍스트는 bold입니다.</b></p>
                <p><strong>이 텍스트는 strong입니다.</strong></p>
                 <p><i>이 텍스트는 italic입니다.</i></p>
                 <p><em>이 텍스트는 emphasized입니다.</em></p>
                 <p><code>이 텍스트는 code입니다.</code></p>
                 <p><sub>subscript</sub>이고 <sup>superscript</sup>입니다.</p>
                 <hr /> <!-- 수평선 태그로, 줄을 그어준다 생각하면 된다(단락 구분시 유용하게 사용)-->
             <a href="http://www.w3.org">W3C컨소시엄</a> <!-- HTML에서 하이퍼 링크는 <a>태그로 정의된다.-->
                 <!-- target은 경로를 상대적으로 지정할 수 있게 해주는 태그로써, 여러 가지 종류가 있다.
                _blank는 새로운 윈도우에서 새 페이지를 연다. _self는 현재 윈도우에 새로운 페이지를 적재한다.
                _parents는 부모 프레임에 새로운 페이지를 적재한다.
                _top은 현재 윈도우에 새로운 페이지를 적재하고 모든 프레임을 취소한다.
                target의 속성 값 중 중요한 것은 위 2가지고, self가 디폴트 값이다. 따라서 새로운 페이지를 열게 해주려면 _blank를 사용하여야한다. 보통 구글에서는 default 값을 많이 사용한다. -->
            <!-- 속성의 이름:href, 속성의 값: 위의 URL-->
            <img src=""> <!-- href는 하이퍼 링크를 달때, 이미지를 달때는 src를 사용한다.-->
            <p> 여기는 <br> 태그를 사용하여 <br>줄을 바꾸었습니다.</p> <!--  br태그는 break로 다음 줄로 넘어가게 하는 역할을 한다. \랑 비슷한 느낌으로 생각하면 된다.-->
            <pre> 텍스트 그대로 출력 </pre>  <!-- <pre>태그를 사용하면 사용자가 입력하는 그대로 출력해준다.-->
                <ul>  <!-- 리스트 태그로써 항목을 나열하는 데 사용된다.--> <!-- ul은 unordered lists로 번호가 없는 리스트이다.-->
                    <!-- ol은 ordered lists로 번호가 있는 리스트이다.--> 
                    <li>cctv</li> <!-- li는 list item의 약자로서 앞에 불릿이 붙는다.-->
                    <li>자동차</li>
                    <li>주차장</li>
                </ul>
                <dl>
                    <dt>에스프레소</dt> <!-- 단어 -->
                    <dd> - 커피의 기본, 커피의 원액이다.</dd> <!-- 단어에 대한 설명 -->
                </dl>
        <form action="input.jsp" method="post">            
            <input type="text" name="input"  /> 
            <input type="submit">
        </form>
       
    </body>
</html>
